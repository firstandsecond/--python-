def InsertionSort(list_before):
    j = 1 
    while(j<len(list_before)):
        i = j
        temp = list_before[i]
        while(i-1>=0):
            if(temp < list_before[i-1]):
                list_before[i] = list_before[i-1]
                list_before[i-1] = temp
            if(temp > list_before[i-1]):
                pass
            i = i - 1   
        j = j + 1
    print(list_before)
InsertionSort(list_before=[9,2,1,8,7,5,3,4])   
